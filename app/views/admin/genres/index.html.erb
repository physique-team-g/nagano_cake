<h3>ジャンル一覧・追加</h3>
<%= form_with model:genres, local:true do |f| %>
  <div class="form-group">
    <%= f.label "ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit"新規登録", class:"btn btn-success" %>
  </div>
<% end %>

